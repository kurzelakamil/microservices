spring:
  application:
    name: first-service
  config:
    name: configserver
  cloud:
    config:
      uri: http://localhost:8888
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: group-id
      properties:
        spring:
          json:
            trusted:
              packages: '*'
server:
  port: 8080